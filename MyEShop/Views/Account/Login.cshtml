@model MyEShop.Models.ViewModel.LoginViewModel

@{
    ViewData["Title"] = "Login";
}
@if (User.Identity.IsAuthenticated)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        شما دسترسی ندارید
    </div>
}
@* <h4>ورود</h4>
<hr />
<div class="row">
    <div class="col-md-8">
        <form asp-action="Login">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Password" class="control-label"></label>
                <input asp-for="Password" class="form-control" />
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
          
            <div class="form-group d-flex justify-content-start align-items-center gap-2 my-2">
                <input class="form-check-input m-0" type="checkbox" asp-for="RememberMe" id="RememberMe" />
                <label class="form-check-label m-0" for="RememberMe">
                    مرا به یاد داشته باش
                </label>
            </div>
            
            <div class="form-group">
                <input type="submit" value="ورود" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div> *@

<!-- login Start -->
@await Html.PartialAsync("Partials/__Login")
<!-- login Start -->

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
